<UserControl x:Class="AutoFileMover.Desktop.Views.EngineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBlock Text="{Binding State}" Margin="10,10,10,266"/>
        <Button Content="Start" HorizontalAlignment="Left" Margin="10,268,0,0" VerticalAlignment="Top" Width="75" Command="{Binding Start}"/>
        <Button Content="Stop" HorizontalAlignment="Left" Margin="90,268,0,0" VerticalAlignment="Top" Width="75" Command="{Binding Stop}"/>
    </Grid>
</UserControl>
