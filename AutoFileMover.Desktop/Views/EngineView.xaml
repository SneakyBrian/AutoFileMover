<UserControl x:Class="AutoFileMover.Desktop.Views.EngineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding State}"/>
        <ListView ItemsSource="{Binding FileOperations}" Grid.Row="1" />
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button Content="Start" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Command="{Binding Start}"/>
            <Button Content="Stop" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Command="{Binding Stop}"/>
            <Button Content="Scan" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Command="{Binding Scan}"/>
        </StackPanel>
    </Grid>
</UserControl>
